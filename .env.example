# ============================================================================
# SPITE — environment variable template
# ----------------------------------------------------------------------------
# Copy this file to `.env.local` and fill in real values.
# `.env.local` is gitignored — it will never be committed.
# After editing, restart the app for changes to take effect.
# ============================================================================

# --- Database (Neon Postgres) -----------------------------------------------
# Where: neon.tech → your project → "Connection string"
# Must start with postgresql://
DATABASE_URL=""

# --- Cloudflare R2 storage --------------------------------------------------
# Where: dash.cloudflare.com → R2 → API tokens
# Account ID from the right sidebar of any R2 page.
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""

# The R2 bucket name you created. Anything you like.
R2_BUCKET_NAME=""

# Leave EMPTY for the default secure setup. Uploads are served through the
# /api/r2-image proxy with HMAC-signed URLs — the bucket stays private.
# Only set this if you've connected a custom public domain to the bucket.
R2_PUBLIC_URL=""

# --- Optional: a different S3-compatible store ------------------------------
# Most people should leave BOTH of these empty. That uses Cloudflare R2 — the
# default, and the only path the setup page walks you through.
# Only set S3_ENDPOINT if you're comfortable making S3 credentials and CORS
# rules yourself. It points SPITE at MinIO, Backblaze B2 or AWS S3 instead, and
# R2_ACCOUNT_ID is then ignored. The keys and bucket name above still apply.
# See "Other storage providers" in the README before using this.
S3_ENDPOINT=""
# Only for providers needing a real region (AWS S3). Defaults to 'auto' (R2).
S3_REGION=""

# --- fal.ai (AI generation provider) ----------------------------------------
# Where: fal.ai → Dashboard → Keys
# Format: <uuid>:<hex>
FAL_KEY=""

# --- Agent chat (optional) --------------------------------------------------
# The canvas Agent talks through fal's OpenRouter-compatible endpoint using
# FAL_KEY. Override the model or point at another OpenAI-compatible API:
# AGENT_MODEL="google/gemini-3.8-flash"
# AGENT_API_KEY=""
# AGENT_BASE_URL=""

# --- Clerk (login) ----------------------------------------------------------
# dashboard.clerk.com → your app → API keys
# App will refuse to boot if these are empty.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
CLERK_SECRET_KEY=""
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/

# --- Optional ---------------------------------------------------------------
# Used by the scheduled cleanup job at /api/assets/cleanup. Only needed if
# you've wired up a cron (Vercel Cron, GitHub Actions, etc.) to call that
# route. Any long random string works.
CRON_SECRET=""

# Public base URL of your deployment, used when SPITE generates signed
# reference URLs to send to fal.ai. Only needed if you're hosting outside
# Vercel — on Vercel, SPITE automatically uses VERCEL_PROJECT_PRODUCTION_URL
# so fal can fetch references through your public production URL even when
# you're testing on a Deployment-Protected preview. Set this if you have a
# custom domain or non-Vercel host.
# Format: https://your-domain.com  (no trailing slash)
# SITE_URL=""

# Optional kill switch. Set to "1" to refuse all new generation submissions
# from the canvas — useful if you suspect a runaway billing situation.
# Leave unset or empty for normal operation.
# GENERATION_DISABLED=""

# Optional per-hour USD ceiling enforced server-side on generation
# submissions. Default $100/hour — fits a working AI-filmmaker session
# with room for video batches while still capping a runaway loop or
# compromised cookie at a bounded loss. Raise for unusual projects, set
# to 0 to disable entirely.
# SPEND_LIMIT_USD_PER_HOUR=""

# Optional hard cap on the ESTIMATED cost of any single submission. Disabled
# by default. The per-hour ceiling above is only as accurate as the price
# table in lib/fal-cost.ts (some entries are flat-rate and don't scale with
# resolution/duration), so this is a blunt backstop against one oversized or
# under-priced generation slipping through while the hour still has budget.
# Set to a dollar amount to enable (e.g. 50).
# SPEND_LIMIT_USD_PER_REQUEST=""

# Optional global cap on failed login attempts per minute, across ALL IPs.
# Default 60. This is the backstop that makes online password brute-force
# impractical even on hosts where the per-IP limit can be evaded by spoofing
# the X-Forwarded-For header (i.e. any host that doesn't set a trusted
# X-Real-IP — Vercel does, most bare hosts don't). Set to 0 to disable the
# global cap (NOT recommended unless you fully trust your proxy's client IP).
# LOGIN_RATE_LIMIT_GLOBAL_MAX=""

# Optional dedicated HMAC signing secret for the legacy /api/r2-image
# path-token URLs. The current fal.ai flow uses R2 presigned URLs and does
# NOT need this. Only set it if you specifically rely on the path-token proxy
# form; when unset, that token path is simply disabled (cookie auth still
# works). There is intentionally no APP_PASSWORD fallback — don't reuse your
# login password as a signing key.
# R2_PROXY_SIGNING_SECRET=""
